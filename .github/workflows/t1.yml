name: t1

on:
  push:
    branches: [main]
  pull_request:

jobs:
  t1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: fake harness markers (so the installer has something to install into)
        run: mkdir -p ~/.claude ~/.omp/agent/skills
      - name: node syntax + T-key parity
        run: |
          node --check bin/exuvia.js
          python tests/parity.py
      - name: deterministic test rig (T1)
        run: python tests/run.py --t1
