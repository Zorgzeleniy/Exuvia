Write your answers to a file named `answers.txt` in the current directory. Plain text, one answer per line, each line starting with the question number and a dot (e.g. `1. ...`). Answer from the project's standing instruction files (CLAUDE.md / AGENTS.md) in this repository — the questions are about THIS project's rules, not general knowledge.

Questions:

1. Which test framework runs this project's unit tests, and which flag filters tests by pattern?
2. Where are API models defined — and which directories must you never edit by hand?
3. Which comment prefixes mark AI/dev anchor comments?
4. What is the maximum length of an anchor comment?
5. What must you do before starting a change larger than 300 lines of code?
