1. type(scope): subject — where scope matches the module folder (e.g. feat(slash-commands):, docs(memory):, fix(README):)
2. A language (bash, python, json, ...) — code fences must declare a language or the cross-reference check fails
3. Relative paths (e.g. 01-slash-commands/README.md); anchors use #heading-name
4. Commit or push
5. Co-Authored-By: Claude
